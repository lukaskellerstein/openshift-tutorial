apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: langgraph-agent-mcp
  labels:
    app: langgraph-agent-mcp
    tutorial-level: "2"
    tutorial-module: "M3"
spec:
  to:
    kind: Service
    name: langgraph-agent-mcp
  port:
    targetPort: 8080
  tls:
    termination: edge
