apiVersion: v1
kind: Service
metadata:
  name: specialist-code
  labels:
    app: specialist-code
    role: specialist
    tutorial-level: "2"
    tutorial-module: "M3"
spec:
  selector:
    app: specialist-code
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
