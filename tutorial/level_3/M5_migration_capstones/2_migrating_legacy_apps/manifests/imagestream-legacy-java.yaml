# ImageStream for the legacy Java application (binary build)
# This tracks the built image in the internal registry
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: legacy-java-app
  labels:
    app: legacy-java-app
    tutorial-level: "3"
    tutorial-module: "M5"
    migration-strategy: lift-and-shift
