apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-ui
  labels:
    app: shopinsights
    component: dashboard-ui
    tutorial: personalized
    lesson: "01"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shopinsights
      component: dashboard-ui
  template:
    metadata:
      labels:
        app: shopinsights
        component: dashboard-ui
    spec:
      containers:
        - name: dashboard-ui
          image: ghcr.io/<your-username>/openshift-tutorial/shopinsights-dashboard:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
