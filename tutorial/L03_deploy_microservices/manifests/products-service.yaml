apiVersion: v1
kind: Service
metadata:
  name: products-service
  labels:
    app: shopinsights
    component: products-service
    tutorial: personalized
    lesson: "03"
spec:
  selector:
    app: shopinsights
    component: products-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
