apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  labels:
    app: shopinsights
    component: analytics-service
    tutorial: personalized
    lesson: "03"
spec:
  selector:
    app: shopinsights
    component: analytics-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
