# Reset the OAuth configuration to its default (no identity providers)
# Used during cleanup to remove the HTPasswd provider configured in this lesson.
apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
  labels:
    app: auth-tutorial
    tutorial-level: "1"
    tutorial-module: "M2"
spec:
  identityProviders: []
