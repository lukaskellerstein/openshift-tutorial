# ImageStream for the Python sample application
# Used as the output target for the Python S2I BuildConfig
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: python-app
  labels:
    app: python-app
    tutorial-level: "1"
    tutorial-module: "M3"
