apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: orders-service
  labels:
    app: shopinsights
    component: orders-service
    tutorial: personalized
    lesson: "02"
spec:
  source:
    type: Git
    git:
      uri: https://github.com/<your-username>/openshift-tutorial.git
    contextDir: tutorial/shared_app/orders-service
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: orders-service:latest
  triggers:
    - type: ConfigChange
