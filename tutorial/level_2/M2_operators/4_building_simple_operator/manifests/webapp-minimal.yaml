# Minimal WebApp CR — uses all defaults (1 replica, port 8080, route enabled)
apiVersion: tutorial.openshift.io/v1alpha1
kind: WebApp
metadata:
  name: minimal-web
  labels:
    app: minimal-web
    tutorial-level: "2"
    tutorial-module: "M2"
spec:
  image: quay.io/redhattraining/hello-world-nginx:latest
