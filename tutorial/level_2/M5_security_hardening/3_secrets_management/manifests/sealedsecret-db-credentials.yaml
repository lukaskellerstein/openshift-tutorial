# SealedSecret — encrypted Secret safe for Git
# Generated by: kubeseal --format yaml < secret.yaml > sealedsecret-db-credentials.yaml
# The encryptedData values below are EXAMPLES — you must generate your own
# using kubeseal against your cluster's Sealed Secrets controller.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: db-credentials
  namespace: secrets-management-lab
  labels:
    app: secret-demo
    tutorial-level: "2"
    tutorial-module: "M5"
spec:
  encryptedData:
    # These are placeholder values — real encrypted data is a long base64 string
    # generated by kubeseal using the controller's public key.
    username: AgBOz3F...REPLACE_WITH_KUBESEAL_OUTPUT...==
    password: AgCx7K2...REPLACE_WITH_KUBESEAL_OUTPUT...==
  template:
    metadata:
      name: db-credentials
      namespace: secrets-management-lab
      labels:
        app: secret-demo
        tutorial-level: "2"
        tutorial-module: "M5"
    type: Opaque
