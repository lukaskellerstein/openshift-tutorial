# Service for the secret-demo deployment
apiVersion: v1
kind: Service
metadata:
  name: secret-demo
  labels:
    app: secret-demo
    tutorial-level: "2"
    tutorial-module: "M5"
spec:
  selector:
    app: secret-demo
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
