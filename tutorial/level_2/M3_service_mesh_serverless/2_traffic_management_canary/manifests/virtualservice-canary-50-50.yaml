# Canary Phase 2: 50% to v1, 50% to v2.
# The canary has passed initial validation; increase exposure.
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: canary-demo
  labels:
    app: canary-demo
    tutorial-level: "2"
    tutorial-module: "M3"
spec:
  hosts:
    - canary-demo.bookinfo-mesh.svc.cluster.local
    - "canary-demo.bookinfo-mesh.apps-crc.testing"
  gateways:
    - canary-demo-gateway
    - mesh
  http:
    - route:
        - destination:
            host: canary-demo.bookinfo-mesh.svc.cluster.local
            subset: v1
            port:
              number: 8080
          weight: 50
        - destination:
            host: canary-demo.bookinfo-mesh.svc.cluster.local
            subset: v2
            port:
              number: 8080
          weight: 50
