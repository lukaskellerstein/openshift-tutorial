apiVersion: v1
kind: Service
metadata:
  name: analytics-service-v1
  namespace: shopinsights
  labels:
    app: shopinsights
    component: analytics-service
    tutorial: personalized
    lesson: "05"
spec:
  selector:
    app: shopinsights
    component: analytics-service
    version: v1
  ports:
    - port: 8080
      targetPort: 8080
