# UIPlugin — enables the distributed tracing view in the OpenShift Console
# Adds Observe > Traces page that queries TempoMonolithic instances with multitenancy
apiVersion: observability.openshift.io/v1alpha1
kind: UIPlugin
metadata:
  name: distributed-tracing
spec:
  type: DistributedTracing
